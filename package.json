{
  "name": "base64encoding",
  "version": "1.0.0",
  "description": "This is a base64 script that takes images & text and converts them to base64 string for easier storage and transfer",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Onejohi",
  "license": "ISC",
  "dependencies": {
    "notarealdb": "^0.2.2"
  }
}
